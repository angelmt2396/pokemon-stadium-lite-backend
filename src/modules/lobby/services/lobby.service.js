import { AppError } from '../../../shared/errors/AppError.js';

export const joinLobby = async () => {
  throw new AppError('Lobby join flow is not implemented yet', 501);
};

export const markPlayerReady = async () => {
  throw new AppError('Lobby ready flow is not implemented yet', 501);
};
